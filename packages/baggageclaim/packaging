set -e

REPO_NAME=github.com/concourse/baggageclaim

source /var/vcap/packages/golang-1.13-linux/bosh/compile.env

export GOPATH=$BOSH_INSTALL_TARGET

mkdir ${BOSH_INSTALL_TARGET}/src
cp -a * ${BOSH_INSTALL_TARGET}/src

go install ${REPO_NAME}/cmd/baggageclaim
go install ${REPO_NAME}/cmd/fs_mounter
