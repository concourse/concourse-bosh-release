---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: ubuntu

inputs:
- name: fly-rc
- name: concourse

outputs:
- name: fly-rc-unpacked

run:
  path: concourse/ci/scripts/fly-unpack-release
